<thlogic>
    <attr sel="#headId"
          th:replace="~{fragments/head :: headFragment}"/>
    <attr sel="#welcome" th:text="#{game.welcome}"/>
    <attr sel="#play-game"
          th:href="@{__${T(com.springtutorial.util.GameMappings).PLAY}__}"
          th:text="Play"/>
    <attr sel="#footerId"
          th:replace="~{fragments/footer :: footer}"/>
</thlogic>